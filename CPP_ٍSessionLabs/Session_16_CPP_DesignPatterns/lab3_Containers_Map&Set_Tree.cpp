

#include <iostream>
#include <list>
#include <algorithm>
#include <iterator>
// #include <map>
#include <set>
// using namespace map;
using namespace std;

int main()
{

    set<int> numbers = {1,100,10,70,100};
    multiset<int> multi_numbers;












    // map<string, int> myMap;                              // Create a map to hold string keys and int values
    // myMap.insert(make_pair<string, int>("Mahmoud", 25)); // Insert a key-value pair into the map
    // myMap.insert(pair<string, int>("Ahmed", 30));        // Another way to insert a key-value pair
    // myMap.emplace("Abou Treka", 43);

    // map<string, int>::iterator it;
    // for (it = myMap.begin(); it != myMap.end(); ++it)
    // {
    //     cout << "Key: " << it->first << ", Value: " << it->second << endl; // Print each key-value pair
    // }

    // // empty map container.
    // map<int, int> quiz;

    // quiz.insert(pair<int, int>(1, 40)); // make_pair(1,40s)
    // quiz.insert(pair<int, int>(2, 30)); // make_pair(1,30s)
    // quiz.insert(pair<int, int>(3, 50)); // make_pair(1,50s)
    // quiz.insert(pair<int, int>(4, 20)); // make_pair(1,40s)
    // quiz.insert(pair<int, int>(5, 50)); // make_pair(1,40s)
    // quiz.insert(pair<int, int>(6, 50)); // make_pair(1,40s)

    // quiz[7] = 10; // another way of inserting a value in a map (7, 10) by indexing.

    // // printing map quiz
    // map<int, int>::iterator itr;
    // cout << "\nThe map quiz is : \n";
    // cout << "\tKEY\tELEMENT\n";
    // for (itr = quiz.begin(); itr != quiz.end(); ++it)
    // {
    //     cout << "Key: " << it->first << ", Value: " << it->second << endl; // Print each key-value pair
    // }
    // cout << endl;

    return 0;
}