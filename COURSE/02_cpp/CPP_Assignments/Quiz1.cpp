#include <iostream>
#include <string>

int main()
{

    std::string s {"C++ Quiz!"};
    // std::cout << s.substr(4, 4) << std::endl; // Print substring directly.
    // std::string sub = s.substr(4, 4); // Store the substring
    // std::cout << sub << std::endl;    // Print the substring
    std::cout << "s =\t" << s << std::endl;

    return 0;
}
